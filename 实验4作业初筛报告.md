# 实验4作业初筛报告

## 提交完整性

所有计划中的组件均已启动并运行。LLM Router 已成功生成四智能体协同计划，覆盖 OC-DeepSeek、OC-Qwen、Hermes-Qwen 三组审查角色及学生反馈生成链路。各组提交物均已产出可供初筛的中间结果或运行证据。未发现组件缺失或未启动的情况。

## 双 Gateway 检查

Hermes 两侧 Gateway 均已运行：

- **Hermes exp4deepseek Gateway**：已启动，通道正常，负责驱动 DeepSeek 侧智能体编排。
- **Hermes exp4qwen Gateway**：已启动，通道正常，负责驱动 Qwen 侧智能体编排。

两个 Gateway 的状态均为在线，未发现连接异常或端口冲突。

## 四机器人与飞书检查

OpenClaw 侧两个实验实例均已完成飞书账户绑定：

- **OpenClaw exp4-deepseek**：已绑定飞书 account，消息通道可正常收发。
- **OpenClaw exp4-qwen**：已绑定飞书 account，消息通道可正常收发。

四智能体角色（OC-DeepSeek、OC-Qwen、Hermes-Qwen 以及 LLM Router 下的学生反馈 Agent）均已就位，与飞书通道的对接验证通过。

## 本地 Qwen/no-thinking 检查

OC-Qwen 已通过 OpenClaw 调用本地 qwen3.6 Agent 成功生成学生反馈。关键运行参数与结果：

- 使用 `/no_think` 指令，搭配 `thinking off` 参数。
- 总耗时约 **108 秒**。
- 生成过程中**无推理过程输出**（no thinking trace），符合预期行为。
- 输出内容为学生反馈文本，生成链路正常。

## 安全与隐私风险

Hermes-Qwen 已完成安全复核，识别出以下隐私风险点并要求全部脱敏：

- **API Key**：不得以明文出现在日志、提交物或截图中。
- **App Secret**：须在提交前完全移除或替换为占位符。
- **chat_id**：飞书会话 ID 须脱敏处理。
- **open_id**：飞书用户 open_id 须脱敏处理。

上述四项均为高优先级补证项，未完成脱敏的提交将被视为不合格。

## OC-DeepSeek 工程配置审查

OC-DeepSeek 已完成工程配置审查，并提出了具体的补证项。审查覆盖了配置文件完整性、环境变量管理、密钥引用方式等方面。详细补证清单由 OC-DeepSeek 出具，作业提交者需逐项整改并附整改说明。

## 需要补交的问题

1. **隐私脱敏**（阻塞项）：API Key、App Secret、chat_id、open_id 须全部替换为占位符，并附脱敏前后对照说明。
2. **配置文件补证**（阻塞项）：OC-DeepSeek 提出的工程配置补证项须逐条整改，提供整改后的配置文件及整改说明。
3. **运行日志完整性**：建议补充一次完整的端到端运行日志（含 LLM Router → 三智能体审查 → 学生反馈生成的全链路），以验证协同流程的稳定性。

## 授课老师验收摘要

| 验收项 | 状态 | 说明 |
|--------|------|------|
| LLM Router 四智能体计划 | ✅ 通过 | 计划已生成，角色分工明确 |
| 双 Gateway 运行 | ✅ 通过 | exp4deepseek 与 exp4qwen 均在线 |
| 飞书账户绑定 | ✅ 通过 | 两组 OpenClaw 实例均已绑定 |
| 本地 Qwen /no_think 测试 | ✅ 通过 | qwen3.6 Agent 生成反馈成功，耗时约 108 秒，无推理过程 |
| 安全与隐私复核 | ❌ 待整改 | API Key / App Secret / chat_id / open_id 未脱敏 |
| OC-DeepSeek 配置补证 | ❌ 待整改 | 补证项已提出，尚未完成整改 |
| 端到端全链路日志 | ⚠️ 建议补充 | 目前仅有各组件独立证据，建议补充一次完整联调日志 |

**初筛结论**：功能性验收项全部通过，核心风险集中在隐私脱敏与配置补证两方面。建议在授课老师验收前优先完成两项阻塞整改，并补充全链路日志以增强提交的完整性。
